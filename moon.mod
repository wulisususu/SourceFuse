name = "wulisususu/sourcefuse"

version = "0.1.0"

readme = "README.md"

repository = "https://github.com/wulisususu/SourceFuse"

license = "Apache-2.0"

keywords = [ "reconciliation", "provenance", "conflict-resolution", "decision-trace", "moonbit" ]

description = "Deterministic multi-source value arbitration and explainable reconciliation for MoonBit"

preferred_target = "native"
